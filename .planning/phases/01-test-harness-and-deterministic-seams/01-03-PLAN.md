---
phase: 01-test-harness-and-deterministic-seams
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - xiuxian-2/scripts/services/LevelConfigLoader.cs
  - xiuxian-2/scripts/adapters/godot/GodotConfigSource.cs
  - xiuxian-2/scripts/adapters/godot/GodotRandomAdapter.cs
  - xiuxian-2/scripts/adapters/godot/SystemClock.cs
  - tests/Xiuxian2.Core.Tests/Services/LevelConfigLoaderSeamTests.cs
autonomous: true
requirements:
  - TEST-06
must_haves:
  truths:
    - "LevelConfigLoader behavior can be driven under tests with scripted config input, RNG output, and time values."
    - "The autoload keeps its current runtime-facing API while using explicit seams internally."
    - "Tests do not need to open live config files or rely on nondeterministic random rolls to verify loader behavior."
  artifacts:
    - path: xiuxian-2/scripts/services/LevelConfigLoader.cs
      provides: "Seam-aware autoload facade"
    - path: xiuxian-2/scripts/adapters/godot/GodotConfigSource.cs
      provides: "Runtime config adapter"
    - path: xiuxian-2/scripts/adapters/godot/GodotRandomAdapter.cs
      provides: "Runtime RNG adapter"
    - path: xiuxian-2/scripts/adapters/godot/SystemClock.cs
      provides: "Runtime clock adapter"
    - path: tests/Xiuxian2.Core.Tests/Services/LevelConfigLoaderSeamTests.cs
      provides: "TDD coverage for deterministic loader behavior"
  key_links:
    - from: xiuxian-2/scripts/services/LevelConfigLoader.cs
      to: xiuxian-2/scripts/contracts/IConfigSource.cs
      via: "config text loading seam"
      pattern: "IConfigSource"
    - from: xiuxian-2/scripts/services/LevelConfigLoader.cs
      to: xiuxian-2/scripts/contracts/IRng.cs
      via: "drop/random seam"
      pattern: "IRng"
    - from: xiuxian-2/scripts/services/LevelConfigLoader.cs
      to: xiuxian-2/scripts/contracts/IClock.cs
      via: "time-sensitive cap and pity seam"
      pattern: "IClock"
---

<objective>
Make `LevelConfigLoader` deterministic enough for repeatable service-level tests.

Purpose: cover the highest-value seam target in Phase 1 and prove TEST-06 for config source, randomness, and time boundaries without changing the autoload contract consumed by the game.
Output: a seam-aware `LevelConfigLoader`, Godot-backed runtime adapters, and focused TDD coverage for deterministic loader behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-test-harness-and-deterministic-seams/01-RESEARCH.md
@.planning/phases/01-test-harness-and-deterministic-seams/01-02-PLAN.md
@xiuxian-2/scripts/services/LevelConfigLoader.cs

<interfaces>
From `xiuxian-2/scripts/contracts/IConfigSource.cs`:
```csharp
public interface IConfigSource
{
    string ReadAllText(string path);
    bool Exists(string path);
}
```

From `xiuxian-2/scripts/contracts/IRng.cs`:
```csharp
public interface IRng
{
    int NextInt(int minInclusive, int maxExclusive);
    double NextDouble();
}
```

From `xiuxian-2/scripts/contracts/IClock.cs`:
```csharp
public interface IClock
{
    DateTime UtcNow { get; }
}
```
</interfaces>

Preserve public Godot-facing members on `LevelConfigLoader`; add seam adoption internally or through explicit test-visible injection points rather than breaking call sites.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: write failing tests for deterministic loader seams</name>
  <files>tests/Xiuxian2.Core.Tests/Services/LevelConfigLoaderSeamTests.cs</files>
  <behavior>
    - Test 1: loader config parsing can be driven from an in-memory config source instead of `FileAccess.Open`.
    - Test 2: repeated drop-related behavior can be controlled by scripted RNG output.
    - Test 3: daily/hourly or time-sensitive counters can be validated against a fixed clock value.
  </behavior>
  <action>Write failing tests around the exact deterministic seams Phase 1 needs from `LevelConfigLoader`. Target stable externally observable behavior and avoid broad characterization of the whole 2000+ line service in this plan.</action>
  <verify>
    <automated>dotnet test tests/Xiuxian2.Core.Tests/Xiuxian2.Core.Tests.csproj --filter FullyQualifiedName~LevelConfigLoaderSeamTests</automated>
  </verify>
  <done>The core test project contains failing tests that specify how `LevelConfigLoader` must behave once config, RNG, and clock seams are introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: refactor LevelConfigLoader behind config, rng, and clock seams</name>
  <files>xiuxian-2/scripts/services/LevelConfigLoader.cs, xiuxian-2/scripts/adapters/godot/GodotConfigSource.cs, xiuxian-2/scripts/adapters/godot/GodotRandomAdapter.cs, xiuxian-2/scripts/adapters/godot/SystemClock.cs</files>
  <action>Implement the minimum seam-aware refactor needed to pass the new tests: move file text acquisition behind `IConfigSource`, route random behavior through `IRng`, and route time-sensitive logic through `IClock`. Provide Godot-backed adapters for runtime defaults and keep `_Ready()`/autoload behavior intact. Do not widen this into DTO extraction or full config contract freezing yet; that belongs to later phases.</action>
  <verify>
    <automated>dotnet test tests/Xiuxian2.Core.Tests/Xiuxian2.Core.Tests.csproj --filter FullyQualifiedName~LevelConfigLoaderSeamTests</automated>
  </verify>
  <done>`LevelConfigLoader` can be exercised with deterministic inputs under tests while preserving existing runtime-facing behavior for the game.</done>
</task>

</tasks>

<commit_strategy>
1. `test(01-03): specify LevelConfigLoader seam behavior`
2. `feat(01-03): inject config rng and clock seams into LevelConfigLoader`
</commit_strategy>

<verification>
Run the targeted loader seam tests after each step, then run the full solution command once the refactor passes to confirm no harness regressions were introduced.</verification>

<success_criteria>
`LevelConfigLoader` no longer forces tests through live file IO, runtime randomness, or wall-clock behavior, and the resulting deterministic seam coverage passes through the default CLI harness.
</success_criteria>

<output>
After completion, create `.planning/phases/01-test-harness-and-deterministic-seams/01-03-SUMMARY.md`
</output>
