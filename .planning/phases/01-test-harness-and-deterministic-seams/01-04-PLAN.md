---
phase: 01-test-harness-and-deterministic-seams
plan: "04"
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - xiuxian-2/scripts/services/CloudSaveSyncService.cs
  - xiuxian-2/scripts/adapters/godot/GodotFileSystem.cs
  - tests/Xiuxian2.Core.Tests/Services/CloudSaveSyncServiceSeamTests.cs
autonomous: true
requirements:
  - TEST-06
must_haves:
  truths:
    - "CloudSaveSyncService tests can read and write save payloads without touching a real user save path."
    - "Upload and download behavior can be asserted through fake filesystem and cloud bridge inputs."
    - "The runtime service still defaults to Godot-backed behavior outside tests."
  artifacts:
    - path: xiuxian-2/scripts/services/CloudSaveSyncService.cs
      provides: "Filesystem-seamed runtime service"
    - path: xiuxian-2/scripts/adapters/godot/GodotFileSystem.cs
      provides: "Runtime filesystem adapter"
    - path: tests/Xiuxian2.Core.Tests/Services/CloudSaveSyncServiceSeamTests.cs
      provides: "TDD coverage for save-path and cloud boundary behavior"
  key_links:
    - from: xiuxian-2/scripts/services/CloudSaveSyncService.cs
      to: xiuxian-2/scripts/contracts/IFileSystem.cs
      via: "save path read/write seam"
      pattern: "IFileSystem"
---

<objective>
Make `CloudSaveSyncService` safe to test without real filesystem side effects.

Purpose: satisfy the filesystem/runtime portion of TEST-06 by removing reliance on live `user://` save paths in automated tests.
Output: a seam-aware cloud save service, a Godot-backed filesystem adapter, and focused TDD coverage for upload/download behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/research/PITFALLS.md
@.planning/phases/01-test-harness-and-deterministic-seams/01-RESEARCH.md
@.planning/phases/01-test-harness-and-deterministic-seams/01-02-PLAN.md
@xiuxian-2/scripts/services/CloudSaveSyncService.cs

<interfaces>
From `xiuxian-2/scripts/contracts/IFileSystem.cs`:
```csharp
public interface IFileSystem
{
    bool Exists(string path);
    byte[] ReadAllBytes(string path);
    void WriteAllBytes(string path, byte[] data);
    string GlobalizePath(string path);
}
```
</interfaces>

Keep the existing cloud bridge behavior and reflection-first runtime strategy, but move actual file reads/writes behind `IFileSystem` so tests never touch real user save data.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: write failing tests for cloud save filesystem seams</name>
  <files>tests/Xiuxian2.Core.Tests/Services/CloudSaveSyncServiceSeamTests.cs</files>
  <behavior>
    - Test 1: download writes bytes to a fake globalized path instead of the live filesystem.
    - Test 2: upload reads bytes from a fake filesystem and forwards them to the cloud bridge when enabled.
    - Test 3: disabled or unavailable cloud conditions short-circuit without filesystem side effects.
  </behavior>
  <action>Write failing tests that lock down filesystem-seamed behavior for upload/download flows. Reuse the deterministic support layer from Plan 02; do not create one-off fakes in this plan.</action>
  <verify>
    <automated>dotnet test tests/Xiuxian2.Core.Tests/Xiuxian2.Core.Tests.csproj --filter FullyQualifiedName~CloudSaveSyncServiceSeamTests</automated>
  </verify>
  <done>The test suite contains failing coverage that proves CloudSaveSyncService must be testable without real disk access.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: refactor CloudSaveSyncService to use the filesystem seam</name>
  <files>xiuxian-2/scripts/services/CloudSaveSyncService.cs, xiuxian-2/scripts/adapters/godot/GodotFileSystem.cs</files>
  <action>Introduce `IFileSystem` usage into `CloudSaveSyncService`, add a Godot-backed adapter for runtime defaults, and preserve the existing reflection-backed cloud bridge strategy. Keep the public runtime API stable and make sure tests can inject the fake filesystem without touching `user://` or host disk paths.</action>
  <verify>
    <automated>dotnet test tests/Xiuxian2.Core.Tests/Xiuxian2.Core.Tests.csproj --filter FullyQualifiedName~CloudSaveSyncServiceSeamTests</automated>
  </verify>
  <done>`CloudSaveSyncService` passes deterministic tests for upload/download behavior without reading or writing the real filesystem.</done>
</task>

</tasks>

<commit_strategy>
1. `test(01-04): specify CloudSaveSyncService filesystem seams`
2. `feat(01-04): inject filesystem seam into CloudSaveSyncService`
</commit_strategy>

<verification>
Run the targeted cloud save seam tests after each step, then re-run the full solution command once the refactor is green.</verification>

<success_criteria>
Cloud save upload/download behavior is testable through the default CLI harness without mutating real save files or relying on host-specific filesystem state.
</success_criteria>

<output>
After completion, create `.planning/phases/01-test-harness-and-deterministic-seams/01-04-SUMMARY.md`
</output>
