---
phase: 01-test-harness-and-deterministic-seams
plan: "05"
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - xiuxian-2/scripts/services/InputHookService.cs
  - xiuxian-2/scripts/adapters/platform/GodotPlatformInfo.cs
  - xiuxian-2/scripts/adapters/platform/Win32HookBackend.cs
  - tests/Xiuxian2.Core.Tests/Services/InputHookServicePlatformTests.cs
autonomous: true
requirements:
  - TEST-06
must_haves:
  truths:
    - "InputHookService fallback and unsupported-platform behavior can be asserted without starting real global hooks."
    - "Platform detection and hook lifecycle decisions are driven through explicit seams."
    - "The default Phase 1 test loop remains safe on non-Windows machines and automation runners."
  artifacts:
    - path: xiuxian-2/scripts/services/InputHookService.cs
      provides: "Platform-seamed hook service"
    - path: xiuxian-2/scripts/adapters/platform/GodotPlatformInfo.cs
      provides: "Runtime platform info adapter"
    - path: xiuxian-2/scripts/adapters/platform/Win32HookBackend.cs
      provides: "Runtime global hook backend adapter"
    - path: tests/Xiuxian2.Core.Tests/Services/InputHookServicePlatformTests.cs
      provides: "TDD coverage for fallback and hook decisions"
  key_links:
    - from: xiuxian-2/scripts/services/InputHookService.cs
      to: xiuxian-2/scripts/contracts/IPlatformInfo.cs
      via: "platform detection seam"
      pattern: "IPlatformInfo"
    - from: xiuxian-2/scripts/services/InputHookService.cs
      to: xiuxian-2/scripts/contracts/IHookBackend.cs
      via: "hook lifecycle seam"
      pattern: "IHookBackend"
---

<objective>
Make `InputHookService` deterministic around platform and hook lifecycle decisions.

Purpose: satisfy the platform boundary portion of TEST-06 without introducing real global input capture into the default automated suite.
Output: a seam-aware `InputHookService`, runtime adapters for platform and Win32 hook behavior, and TDD coverage for fallback/platform decisions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/research/PITFALLS.md
@.planning/phases/01-test-harness-and-deterministic-seams/01-RESEARCH.md
@.planning/phases/01-test-harness-and-deterministic-seams/01-02-PLAN.md
@xiuxian-2/scripts/services/InputHookService.cs

<interfaces>
From `xiuxian-2/scripts/contracts/IPlatformInfo.cs`:
```csharp
public interface IPlatformInfo
{
    string Name { get; }
    bool IsWindows { get; }
}
```

From `xiuxian-2/scripts/contracts/IHookBackend.cs`:
```csharp
public interface IHookBackend
{
    bool TryStart(out string errorMessage);
    void Stop();
    bool IsActive { get; }
}
```
</interfaces>

Keep the current in-app fallback behavior and signal surface, but move platform inspection and hook start/stop decisions behind explicit seams so the test suite never invokes real Win32 hooks.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: write failing tests for platform and hook fallback behavior</name>
  <files>tests/Xiuxian2.Core.Tests/Services/InputHookServicePlatformTests.cs</files>
  <behavior>
    - Test 1: unsupported-platform startup emits the expected degraded-mode behavior without trying to start a real hook backend.
    - Test 2: hook backend failures keep fallback behavior deterministic and observable.
    - Test 3: successful backend start flips the service into active mode through the seam rather than direct Win32 calls in the test process.
  </behavior>
  <action>Write failing tests that specify the platform and hook decisions the service must make. Target deterministic behavior around fallback and activation, not full runtime signal choreography or live input capture.</action>
  <verify>
    <automated>dotnet test tests/Xiuxian2.Core.Tests/Xiuxian2.Core.Tests.csproj --filter FullyQualifiedName~InputHookServicePlatformTests</automated>
  </verify>
  <done>The suite contains failing platform/hook tests that can be satisfied without running real global hooks.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: refactor InputHookService behind platform and hook backend seams</name>
  <files>xiuxian-2/scripts/services/InputHookService.cs, xiuxian-2/scripts/adapters/platform/GodotPlatformInfo.cs, xiuxian-2/scripts/adapters/platform/Win32HookBackend.cs</files>
  <action>Implement `IPlatformInfo` and `IHookBackend` adoption inside `InputHookService`, add runtime adapters that preserve the existing Windows hook behavior, and keep the public Godot-facing API/signals stable. Avoid starting real hooks during tests and keep the default Phase 1 suite safe on Linux and CI.</action>
  <verify>
    <automated>dotnet test tests/Xiuxian2.Core.Tests/Xiuxian2.Core.Tests.csproj --filter FullyQualifiedName~InputHookServicePlatformTests</automated>
  </verify>
  <done>`InputHookService` can be verified under tests for platform and fallback decisions without invoking real Win32 hooks, while runtime defaults still use the real adapters.</done>
</task>

</tasks>

<commit_strategy>
1. `test(01-05): specify InputHookService platform fallback behavior`
2. `feat(01-05): inject platform and hook seams into InputHookService`
</commit_strategy>

<verification>
Run the targeted platform tests after each step, then run the full solution command once the service passes to confirm the default suite remains non-runtime-bound.</verification>

<success_criteria>
Input hook automation no longer blocks the default suite, and the repo can verify unsupported-platform and hook-backend behavior deterministically through the CLI harness.
</success_criteria>

<output>
After completion, create `.planning/phases/01-test-harness-and-deterministic-seams/01-05-SUMMARY.md`
</output>
