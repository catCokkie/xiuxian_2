# 10 TODO（全局维护）

最后更新：2026-03-23

## 使用规则
- 使用 checklist：`- [ ]` 未开始、`- [x]` 已完成。
- 进行中在条目后加 `(doing)`。
- 新增任务必须带“验收标准”。
- 文档变更优先于代码变更。

## P0（当前冲刺）
- [x] 配置校验面板（关卡/怪物/掉落）
目标：运行时可看到权重和、空表、无绑定等配置错误。
验收标准：错误项可定位到 `level_id/monster_id/drop_table_id`，并在 UI 中高亮。
当前实现：子菜单已新增 `配置校验` 页签，支持按范围与当前关卡过滤查看；`LevelConfigLoader` 校验结果可直接定位到 `level_id/monster_id/drop_table_id`，并可在子菜单内直接运行 200/1000 次掉落模拟。

- [x] 掉落模拟结果可视化
目标：展示 200/1000 次模拟的掉落分布与保底触发率。
验收标准：可按 `level_id`、`monster_id` 过滤查看。
当前实现：
- 调试面板支持 `F6` 切关卡过滤、`F7` 切怪物过滤。
- `F9` 运行 `sim200`，`F10` 运行 `sim1000`。
- 输出包含 `avg_lq/avg_in/pity/softSkip/dailyBlock/topDrops`。

- [x] 最近战斗日志面板
目标：记录最近 10 次战斗（怪物、掉落、资源结算）。
验收标准：日志迁移到子菜单左侧 `战斗日志` 页签内查看，切场景/读档后可恢复最近记录。
当前实现：最近战斗日志已接入子菜单左侧 `战斗日志` 页签；主界面/调试主条不再保留战斗日志展示口径；切场景/重启后可从存档恢复最近 10 条记录。

- [x] 最小自动化回归用例
目标：覆盖主循环不回归。
验收标准：至少包含 4 条：输入推进、100%切关、战斗结算、保底/上限。
当前实现：已扩展到战斗开始、生命周期、奖励规则、玩家/怪物/装备属性管线、starter 装备与手动装备闭环；统一入口为 `./run-regression-tests.sh`。

## P1（体验与可维护性）
- [ ] 装备正式内容闭环
目标：把当前“固定奖励 + 背包 + 手动装备”从开发闭环提升为正式玩法闭环。
验收标准：装备来源、库存展示、槽位替换、存档恢复和属性影响对普通玩家可理解且可操作。

- [ ] `UiText` 外置化（可选）
目标：将文案从 C# 常量迁移到 JSON/CSV 资源，支持热更新与多语言。
验收标准：保持当前 key 不变，运行时可切换语言资源。

- [ ] 子菜单设置页结构优化
目标：整理设置分组顺序与信息密度，减少滚动负担。
验收标准：系统/画面/进度三组首屏可读，常用项 2 次点击内可达；预留/实验项有明确标签。
当前实现：`Bug反馈` 已具备问题描述、复制日志路径、导出反馈文件、打开数据目录的最小闭环；设置页已隐藏 demo 无用项并对部分预留能力打标。

- [ ] 主条布局自适应校验
目标：不同宽度下保证核心信息不遮挡。
验收标准：最小宽与最大宽下无重叠；区域名与进度条始终可见。

## P2（后续）
- [ ] Steamworks 接入准备
验收标准：完成 SDK 初始化、回调链路、自检日志。

- [ ] Steam Cloud 冲突策略
验收标准：提供本地优先/云端优先/手动选择三种策略。

- [ ] 发布前安全与防刷复核
验收标准：异常输入、时间跳变、后台高频输入三类场景可稳定降权。

## 建议下一阶段执行顺序（2026-03-23）
1. - [ ] 装备正式内容闭环（第一阶段）
目标：把当前固定奖励 + 背包 + 手动装备提升为普通玩家可理解的正式体验。
验收标准：补齐装备来源说明、背包信息层级、槽位替换反馈、获得后查看/装备路径；不引入完整词条系统也能完成稳定换装闭环。

2. - [ ] 主条布局与 DPI / 多屏校验
目标：验证主条在不同宽度、缩放和显示器场景下始终可读。
验收标准：最小宽/最大宽/常见缩放比下无重叠；区域名、核心状态、探索进度条始终可见。

3. - [ ] 控制器瘦身（战斗显示层继续拆分）
目标：继续降低 `PrototypeRootController` / `ExploreProgressController` 的协调复杂度。
验收标准：将战斗开始/重置/显示刷新中的剩余 UI 决策抽离到更清晰的显示层或规则层，且不降低现有回归覆盖。

4. - [ ] 防刷与异常时间场景补齐
目标：把设计文档中的“时间跳变 / 长时间无焦点高输入”从目标描述推进到可验证实现。
验收标准：新增规则或保护分支，并至少补一轮自动化或可重复人工验证方案。

## 已完成（摘录）
- [x] 输入驱动探索进度（与 AP 解耦）。
- [x] 多关卡配置驱动（`levels[]`）与 100% 自动切关。
- [x] 怪物战斗与掉落配置驱动。
- [x] 保底与日/小时上限规则。
- [x] 子菜单改为单页全宽内容区，左上角关闭按钮。
- [x] UI 文案收口到 `scripts/ui/UiText.cs`。
- [x] 子菜单战斗日志页签与最近 10 场战斗记录恢复。
- [x] 玩家/怪物/装备统一属性管线。
- [x] 战斗开始、回合、生命周期、奖励规则的第一阶段正式化。
- [x] 首通固定装备奖励 + 背包 + 手动装备最小闭环。

## 问题复盘（避免复发）
- [x] Godot 场景文件 BOM 触发解析失败（2026-03-01）
问题现象：`res://scenes/ui/MainBarWindow.tscn:1 Parse Error: Expected '['`，并连锁出现 `Failed loading resource` 与 `Node not found`。
根因：`.tscn` 被保存为 `UTF-8 with BOM`，Godot 文本场景解析器在首字符处失败。
规避规则：
- `.tscn/.tres/.gdshader` 统一使用 `UTF-8 (no BOM)`。
- 修改场景文件后，优先检查文件头字节，禁止出现 `EF BB BF`。
- 场景文本属性必须是完整引号行（`text = "..."`），禁止出现缺失结尾引号的半行文本。
- 提交前执行一次场景快速自检（至少打开主场景并确认无 Parse Error）。
