---
phase: 01-test-harness-and-deterministic-seams
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Directory.Packages.props
  - tests/Xiuxian2.Core.Tests/Xiuxian2.Core.Tests.csproj
  - tests/Xiuxian2.Core.Tests/Usings.cs
  - tests/Xiuxian2.Core.Tests/Smoke/TestHarnessSmokeTests.cs
  - tests/.runsettings
  - xiuxian-2/xiuxian2.sln
  - xiuxian-2/docs/TESTING_AUTOMATION.md
autonomous: true
requirements:
  - TEST-01
  - TEST-02
must_haves:
  truths:
    - "A developer can run one documented CLI command and start the default automated suite."
    - "The solution discovers a dedicated core test project through standard dotnet tooling."
    - "The default test entry point stays Godot-runtime-free for fast local feedback."
  artifacts:
    - path: Directory.Packages.props
      provides: "Pinned test package versions for shared tooling"
    - path: tests/Xiuxian2.Core.Tests/Xiuxian2.Core.Tests.csproj
      provides: "Primary fast test project"
    - path: tests/.runsettings
      provides: "Shared runner configuration for one stable CLI path"
    - path: xiuxian-2/docs/TESTING_AUTOMATION.md
      provides: "Documented local and automation entry point"
  key_links:
    - from: xiuxian-2/xiuxian2.sln
      to: tests/Xiuxian2.Core.Tests/Xiuxian2.Core.Tests.csproj
      via: "solution project reference"
      pattern: "Xiuxian2.Core.Tests"
    - from: xiuxian-2/docs/TESTING_AUTOMATION.md
      to: tests/.runsettings
      via: "documented CLI command"
      pattern: "dotnet test .*--settings tests/.runsettings"
---

<objective>
Establish the authoritative fast test entry point for the repository.

Purpose: satisfy TEST-01 first so every later Phase 1 task can verify work through the same CLI path.
Output: a wired test project, pinned package versions, shared runsettings, and English docs for the single default command.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-harness-and-deterministic-seams/01-RESEARCH.md
@.planning/phases/01-test-harness-and-deterministic-seams/01-VALIDATION.md
@.planning/codebase/TESTING.md
@xiuxian-2/xiuxian2.csproj
@xiuxian-2/scripts/tests/InputSystemTest.cs

The repo currently has no automated C# test framework. Keep this plan focused on a plain `dotnet test` foundation; do not add runtime-bound Godot tests to the default loop yet.
</context>

<tasks>

<task type="auto">
  <name>task 1: scaffold the default core test entry point</name>
  <files>Directory.Packages.props, tests/Xiuxian2.Core.Tests/Xiuxian2.Core.Tests.csproj, tests/.runsettings, xiuxian-2/xiuxian2.sln</files>
  <action>Create the fast Phase 1 harness: add central package pinning, create `tests/Xiuxian2.Core.Tests/` as the primary xUnit v3 project, add it to `xiuxian-2/xiuxian2.sln`, and add `tests/.runsettings` so one standard `dotnet test` path works locally and in automation. Keep the project plain .NET-first; do not require Godot runtime, GdUnit, or headless engine execution in this plan.</action>
  <verify>
    <automated>dotnet test tests/Xiuxian2.Core.Tests/Xiuxian2.Core.Tests.csproj</automated>
  </verify>
  <done>The repository contains a discoverable core test project and `dotnet test tests/Xiuxian2.Core.Tests/Xiuxian2.Core.Tests.csproj` runs successfully.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: add the first failing-then-passing harness smoke tests</name>
  <files>tests/Xiuxian2.Core.Tests/Usings.cs, tests/Xiuxian2.Core.Tests/Smoke/TestHarnessSmokeTests.cs</files>
  <behavior>
    - Test 1: the test project can execute a trivial assertion through the standard runner.
    - Test 2: the harness can locate the repository root or solution-relative paths without ad hoc per-test setup.
    - Test 3: the default suite stays runtime-free and does not require booting Godot.
  </behavior>
  <action>Write the smoke tests first so the new harness proves its own viability, then add the minimal shared usings/path helper support needed to make them pass. Keep the helper surface tiny and generic because richer fixtures/builders are deferred to Plan 02.</action>
  <verify>
    <automated>dotnet test tests/Xiuxian2.Core.Tests/Xiuxian2.Core.Tests.csproj --filter FullyQualifiedName~TestHarnessSmokeTests</automated>
  </verify>
  <done>The core test project contains passing smoke tests that prove the default CLI harness works and can be used by later plans.</done>
</task>

<task type="auto">
  <name>task 3: document the single authoritative test command</name>
  <files>xiuxian-2/docs/TESTING_AUTOMATION.md</files>
  <action>Write English documentation for the default Phase 1 workflow: the single local command, the full-solution command, what each command is for, and the rule that fast service-level tests are the default loop. Explicitly state that Godot runtime tests are not part of the default feedback path in this phase.</action>
  <verify>
    <automated>dotnet test xiuxian-2/xiuxian2.sln --settings tests/.runsettings</automated>
  </verify>
  <done>A developer can copy one documented command from the docs and run the same suite entry point locally or in automation.</done>
</task>

</tasks>

<commit_strategy>
1. `chore(01-01): scaffold xunit core test harness`
2. `test(01-01): add harness smoke coverage`
3. `docs(01-01): document the default test command`
</commit_strategy>

<verification>
Use the documented default command after each task. The plan is only complete when both the direct project command and the full solution command are green from the CLI.</verification>

<success_criteria>
The repo has one English-documented default CLI test command, a real core test project in the solution, and passing smoke coverage that proves the harness can support the rest of Phase 1.
</success_criteria>

<output>
After completion, create `.planning/phases/01-test-harness-and-deterministic-seams/01-01-SUMMARY.md`
</output>
