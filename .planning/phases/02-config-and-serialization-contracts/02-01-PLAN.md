---
phase: 02-config-and-serialization-contracts
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Xiuxian2.Core.Tests/Fixtures/config/phase2-valid-level-config.json
  - tests/Xiuxian2.Core.Tests/Fixtures/config/phase2-invalid-level-config.json
  - tests/Xiuxian2.Core.Tests/Services/LevelConfigLoaderConfigContractTests.cs
  - tests/Xiuxian2.Core.Tests/Builders/ServiceFixtureBuilder.cs
  - xiuxian-2/scripts/services/LevelConfigLoader.cs
autonomous: true
requirements:
  - TEST-03
must_haves:
  truths:
    - "Representative config inputs load into repeatable level and reward state under automated tests."
    - "Representative invalid config inputs produce stable validation failures instead of silent drift."
    - "Config contract regressions fail in the CLI loop before manual QA sees broken runtime behavior."
  artifacts:
    - path: tests/Xiuxian2.Core.Tests/Fixtures/config/phase2-valid-level-config.json
      provides: "Frozen happy-path config contract fixture"
    - path: tests/Xiuxian2.Core.Tests/Fixtures/config/phase2-invalid-level-config.json
      provides: "Frozen invalid config fixture for validation regressions"
    - path: tests/Xiuxian2.Core.Tests/Services/LevelConfigLoaderConfigContractTests.cs
      provides: "Automated regression coverage for parse and validation contracts"
    - path: xiuxian-2/scripts/services/LevelConfigLoader.cs
      provides: "Runtime-facing config contract implementation"
  key_links:
    - from: tests/Xiuxian2.Core.Tests/Services/LevelConfigLoaderConfigContractTests.cs
      to: xiuxian-2/scripts/services/LevelConfigLoader.cs
      via: "fixture-backed deterministic config loading"
      pattern: "LevelConfigLoader.*(LoadConfig|GetValidationEntries|BuildValidationSummary)"
    - from: tests/Xiuxian2.Core.Tests/Fixtures/config/phase2-invalid-level-config.json
      to: xiuxian-2/scripts/services/LevelConfigLoader.cs
      via: "validation regression fixture"
      pattern: "validation|drop_table|monster|level"
---

<objective>
Freeze representative config parsing and validation behavior around `LevelConfigLoader`.

Purpose: satisfy TEST-03 early so later Phase 2 save work can rely on a stable config/runtime contract instead of a moving target.
Output: frozen valid and invalid config fixtures plus a deterministic regression suite that covers parse success, validation entries, and summary behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-config-and-serialization-contracts/02-RESEARCH.md
@.planning/phases/02-config-and-serialization-contracts/02-VALIDATION.md
@.planning/phases/01-test-harness-and-deterministic-seams/01-02-SUMMARY.md
@.planning/phases/01-test-harness-and-deterministic-seams/01-03-SUMMARY.md
@xiuxian-2/scripts/services/LevelConfigLoader.cs
@tests/Xiuxian2.Core.Tests/Builders/ServiceFixtureBuilder.cs
@tests/Xiuxian2.Core.Tests/Services/LevelConfigLoaderSeamTests.cs

<interfaces>
From `xiuxian-2/scripts/services/LevelConfigLoader.cs`:
```csharp
public void UseTestSeams(IConfigSource configSource, IRng rng, IClock clock)
public bool LoadConfig()
public string BuildValidationSummary(int maxLines = 12)
public Godot.Collections.Array<string> GetValidationIssues()
public Godot.Collections.Array<Godot.Collections.Dictionary<string, Variant>> GetValidationEntries()
public Godot.Collections.Dictionary<string, Variant> ToRuntimeDictionary()
public void FromRuntimeDictionary(Godot.Collections.Dictionary<string, Variant> data)
```
</interfaces>

Keep the test path runtime-free. Reuse the existing deterministic seam builder pattern instead of reading `res://docs/design/09_level_monster_drop_sample.json` directly or constructing a live Godot `Node` inside xUnit.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: add frozen config fixtures and failing contract tests</name>
  <files>tests/Xiuxian2.Core.Tests/Fixtures/config/phase2-valid-level-config.json, tests/Xiuxian2.Core.Tests/Fixtures/config/phase2-invalid-level-config.json, tests/Xiuxian2.Core.Tests/Services/LevelConfigLoaderConfigContractTests.cs</files>
  <behavior>
    - Test 1: a representative valid config fixture loads and exposes the expected active level and zero-or-known validation issue state.
    - Test 2: a representative invalid fixture produces stable structured validation entries for broken level/monster/drop-table relationships.
    - Test 3: config contract assertions run through deterministic seams and never depend on mutable repo design assets or Godot runtime boot.
  </behavior>
  <action>Write the contract tests first using test-owned fixtures. Assert both structured outputs (`GetValidationEntries` / `GetValidationIssues`) and a small summary contract (`BuildValidationSummary`) so validation drift cannot hide behind text-only checks. Keep the fixture scope representative, not exhaustive; Phase 2 is freezing the riskiest behavior, not snapshotting every content file.</action>
  <verify>
    <automated>dotnet test tests/Xiuxian2.Core.Tests/Xiuxian2.Core.Tests.csproj --filter FullyQualifiedName~LevelConfigLoaderConfigContractTests --logger "console;verbosity=detailed"</automated>
  </verify>
  <done>Phase-owned config fixtures and a failing-then-passing regression suite exist for valid parsing and invalid validation behavior.</done>
</task>

<task type="auto">
  <name>task 2: make the config contract suite pass without widening runtime scope</name>
  <files>xiuxian-2/scripts/services/LevelConfigLoader.cs, tests/Xiuxian2.Core.Tests/Builders/ServiceFixtureBuilder.cs, tests/Xiuxian2.Core.Tests/Services/LevelConfigLoaderConfigContractTests.cs</files>
  <action>Implement only the minimal loader or fixture-builder changes needed for the new contract tests to pass while preserving the existing autoload-facing API. If direct `LevelConfigLoader` construction is unstable in xUnit, follow the 01-03 pattern and keep the deterministic verification path in a runtime-free helper or seam harness. Do not expand into save-schema work, runtime smoke work, or broad content coverage in this plan.</action>
  <verify>
    <automated>dotnet test tests/Xiuxian2.Core.Tests/Xiuxian2.Core.Tests.csproj --filter FullyQualifiedName~LevelConfigLoaderConfigContractTests|FullyQualifiedName~LevelConfigLoaderSeamTests --logger "console;verbosity=detailed"</automated>
  </verify>
  <done>`LevelConfigLoader` has repeatable regression coverage for representative config parsing and validation contracts, and the focused CLI suite stays green.</done>
</task>

</tasks>

<commit_strategy>
1. `test(02-01): add level config contract fixtures and failing regressions`
2. `feat(02-01): freeze level config validation contracts`
</commit_strategy>

<verification>
Use the focused filtered suite while iterating, then keep the quick project command green before moving on. The plan is only complete when the new config contract tests and the existing seam suite both pass from the CLI.</verification>

<success_criteria>
Representative valid and invalid config inputs are frozen under automated tests, and contract-breaking changes in `LevelConfigLoader` fail fast in the default CLI loop.
</success_criteria>

<output>
After completion, create `.planning/phases/02-config-and-serialization-contracts/02-01-SUMMARY.md`
</output>
