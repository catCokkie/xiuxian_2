---
phase: 01-main-ui-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [scripts/ui/MainBarLayoutController.cs, scenes/ui/MainBarWindow.tscn]
requirements: [UI-01, UI-02, UI-04]
autonomous: true
must_haves:
  - truth: Main bar can be dragged from the handle and non-interactive blank areas without redesigning the shell.
    artifacts: [scripts/ui/MainBarLayoutController.cs, scenes/ui/MainBarWindow.tscn]
  - truth: Main bar controls no longer overlap in a way that blocks core information during normal width ranges.
    artifacts: [scripts/ui/MainBarLayoutController.cs, scenes/ui/MainBarWindow.tscn]
---

<objective>
Stabilize main bar interaction and compact layout behavior.

Purpose: Fix the most visible main-bar drag and overlap problems while preserving the current bottom-docked shell.
Output: Updated drag hit-testing and compact-layout priority rules for the existing main bar.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-main-ui-stabilization/01-CONTEXT.md
@.planning/phases/01-main-ui-stabilization/01-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>task 1: Expand main bar drag capture beyond the handle</name>
  <files>scripts/ui/MainBarLayoutController.cs</files>
  <read_first>
scripts/ui/MainBarLayoutController.cs
scripts/ui/SubmenuWindowController.cs
.planning/phases/01-main-ui-stabilization/01-CONTEXT.md
  </read_first>
  <action>Keep `Chrome/DragHandleButton` drag support, and add drag initiation from non-interactive blank areas in the main bar. Reuse the submenu-style guard idea so interactive controls such as buttons, option buttons, progress-related controls, and rich text areas do not accidentally start dragging.</action>
  <acceptance_criteria>
- `scripts/ui/MainBarLayoutController.cs` contains a dedicated blank-area drag decision path in addition to `OnDragHandleGuiInput`
- `scripts/ui/MainBarLayoutController.cs` contains interaction-guard logic that excludes interactive controls from blank-area drag start
- `scripts/ui/MainBarLayoutController.cs` still emits `LayoutChanged` during/after drag
  </acceptance_criteria>
  <verify>dotnet build xiuxian2.sln</verify>
  <done>Main bar supports both handle drag and blank-area drag without hijacking normal control interaction</done>
</task>

<task type="auto">
  <name>task 2: Tighten main bar compact layout priority</name>
  <files>scripts/ui/MainBarLayoutController.cs scenes/ui/MainBarWindow.tscn</files>
  <read_first>
scripts/ui/MainBarLayoutController.cs
scenes/ui/MainBarWindow.tscn
.planning/phases/01-main-ui-stabilization/01-CONTEXT.md
  </read_first>
  <action>Adjust `UpdateRightAnchoredLayout()` and any necessary scene defaults so the current shell keeps core information readable under compact widths. Preserve bottom docking, preserve the current shell, and prioritize non-overlap of the main progress/breakthrough/zone/realm information over secondary controls.</action>
  <acceptance_criteria>
- `scripts/ui/MainBarLayoutController.cs` contains explicit compact-layout handling for overlap-prone main bar controls
- `scripts/ui/MainBarLayoutController.cs` still keeps `LockToBottom` behavior
- `scenes/ui/MainBarWindow.tscn` remains wired to `MainBarLayoutController.cs`
  </acceptance_criteria>
  <verify>dotnet build xiuxian2.sln</verify>
  <done>Main bar compact behavior is explicitly governed by readable priority rules instead of accidental overlap</done>
</task>
</tasks>

<verification>
- [ ] `dotnet build xiuxian2.sln` succeeds
- [ ] Main bar drag code supports both handle and blank-area drag paths
- [ ] Compact layout logic still preserves the current shell and bottom-docked positioning
</verification>

<success_criteria>
- Main bar drag is easier to use than handle-only behavior
- Main bar overlap risk is reduced without redesigning the shell
</success_criteria>

<output>
After completion, create `.planning/phases/01-main-ui-stabilization/01-01-SUMMARY.md`
</output>
